[package]
name = "lyte"
version = "0.1.0"
authors = ["Taylor Holliday <taylor@audulus.com>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
# We'll need this when we get to the backend.
# inkwell = { git = "https://github.com/audulus/inkwell", branch = "master", features = ["llvm12-0"] }
internment = "0.7.0"
salsa = "0.16.1"
superslice = "1"
cranelift = "0.94.0"
cranelift-module = "0.94.0"
cranelift-jit = "0.94.0"