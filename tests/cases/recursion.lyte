// args: -t
// expected stdout:
// compilation successful
// assert(true)
// assert(true)
// vm execution successful

fact(x: i32) → i32 {

    if x == 1 {
        return 1
    }

    return x * fact(x-1)
}

main {
    assert(fact(4) == 24)
}
