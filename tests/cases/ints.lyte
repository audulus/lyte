

assert(cond: bool) → void

test {
    assert(2+2 == 4)
    assert(2+2 == 2+2)
    assert(1-2 == -1)
    assert(1+1+1+1+1+1+1+1 == 8)
    assert(1+1+1+1+1+1+1*2 == 8)
    assert(2⋅2⋅2⋅2⋅2⋅2 == 64)
}
