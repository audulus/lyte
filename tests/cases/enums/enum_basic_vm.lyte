
// args: -r
// expected stdout:
// assert(true)
// assert(true)
// assert(true)
// vm execution successful

enum Color { Red, Green, Blue }

main {
    var c: Color
    c = .Red
    assert(c == .Red)
    c = .Blue
    assert(c == .Blue)
    assert(c != .Red)
}
