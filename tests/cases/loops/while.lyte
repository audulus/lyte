// args: -t
// expected stdout:
// compilation successful
// assert(true)
// assert(true)
// vm execution successful

main {
    var i: i32
    while i < 100 {
        i = i + 1
    }
    assert(i == 100)
}