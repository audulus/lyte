// args: -c
// expected stdout:
// compilation successful
// hithere

main {
    // set up "hi" followed by nulls using strcpy
    var dst = "          "
    strcpy(dst, "hi")
    strcat(dst, "there")
    println(dst)
}
