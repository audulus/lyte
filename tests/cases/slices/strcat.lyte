// args: -t
// expected stdout:
// compilation successful
// hithere
// hithere
// vm execution successful

main {
    var dst: [i8; 32]
    strcat(dst, "hi")
    strcat(dst, "there")
    println(dst)
}
