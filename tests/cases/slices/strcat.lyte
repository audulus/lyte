// args: -t
// expected stdout:
// executing compiled code
// compilation successful
// hithere
// executing LLVM code
// compilation successful
// hithere
// executing VM code
// hithere
// vm execution successful

main {
    var dst: [i8; 32]
    strcat(dst, "hi")
    strcat(dst, "there")
    println(dst)
}
