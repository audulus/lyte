// args: -r
// expected stdout:
// assert(true)
// assert(true)
// vm execution successful

assert(cond: bool) → void

// Test that values are zero-initialized.
main {
    var x:i32
    assert(x == 0)

    var a : [i32; 1]
    assert(a[0] == 0)
}
