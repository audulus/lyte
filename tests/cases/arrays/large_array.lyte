// args: -t
// expected stdout:
// executing compiled code
// compilation successful
// assert(true)
// executing LLVM code
// compilation successful
// assert(true)
// executing VM code
// assert(true)
// vm execution successful

main {
    // Large array allocation and access
    var a: [i32; 1000]
    a[0] = 1
    a[999] = 42
    assert(a[999] == 42)
}
