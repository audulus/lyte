// args: -t
// expected stdout:
// executing compiled code
// compilation successful
// assert(true)
// executing LLVM code
// compilation successful
// assert(true)
// executing VM code
// assert(true)
// vm execution successful

main {
    var a : [i32; 1]
    a[0] = 42
    assert(a[0] == 42)
}
