// args: -t
// expected stdout:
// executing compiled code
// compilation successful
// assert(true)
// assert(true)
// executing VM code
// assert(true)
// assert(true)
// vm execution successful

main {
    var a: [i32; 1]
    a[0] = 7
    assert(a[0] == 7)
    assert(a.len == 1)
}
