// args: -r
// expected stdout:
// assert(true)
// assert(true)
// vm execution successful


id<T>(x: T) → T { x }

main {
    let x = id(42)
    assert(x == 42)

    let y = id(true)
    assert(y == true)
}
