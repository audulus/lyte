

assert(cond: bool)
putc(c: i8)

print(str: [i8]) {
    var i = 0
    while i < len str {
        putc(str[i])
        i = i + 1
    }
}

println(str: [i8]) {
    print(str)
    putc('\n')
}

var y:i32

test {

    let x = 42
    y = 42
    
    on 1 {
        println("hello from thread 1")
        assert(x == 42)

        // y should be different on each thread
        assert(y == 0)
    }

}
