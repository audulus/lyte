
interface Compare<A> {
    cmp(lhs: typevar A, rhs: typevar A) -> i32
}

sort<T>(array: [typevar T]) -> [typevar T] where Compare<T> {
    // cmp(array[0], array[1])
    array
}

test {
    sort([1,2,3])
}
