// args: -t
// expected stdout:
// compilation successful
// assert(true)
// assert(true)
// assert(true)
// assert(true)
// vm execution successful

var global: i32

main {
    assert(global == 0)
    global = 42
    assert(global == 42)
}
