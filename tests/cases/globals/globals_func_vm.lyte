// args: -r
// expected stdout:
// assert(true)
// assert(true)
// vm execution successful

assert(cond: bool) → void

var global: i32

f {
    global = 42
}

main {
    assert(global == 0)
    f()
    assert(global == 42)
}
