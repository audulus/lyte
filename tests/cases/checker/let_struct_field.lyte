// args: --check
// expected stdout:
// ❌ ../tests/cases/checker/let_struct_field.lyte:9:9: left-hand side of assignment isn't assignable
//         p.x = 99
//             ^
struct Point { x: i32, y: i32 }
main {
    let p = Point(x: 1, y: 2)
    p.x = 99
}
