// args: --check
// expected stdout:
// ❌ ../tests/cases/checker/if_branch_mismatch.lyte:8:32: assignment requires matching types: i32 vs f32
//         if true { x = 1 } else { x = 2.0 }
//                                    ^
f → i32 {
    var x: i32
    if true { x = 1 } else { x = 2.0 }
    return x
}
