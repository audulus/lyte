// expected stdout:
// compilation successful
// assert(true)

struct Point {
    x: f32,
    y: f32
}

length(p: Point) -> f32 {
    sqrt(p.x * p.x + p.y * p.y)
}

main {
    var p: Point
    p.x = 3.0
    p.y = 4.0
    assert(length(p) == 5.0)
}
