// args: -c
// expected stdout:
// compilation successful
// positive

main {
    var x = 42
    let y = x + 1    // immutable binding

    if x > 0 {
        println("positive")
    } else {
        println("non-positive")
    }

    var sum = 0
    for i in 0 .. 10 {
        sum = sum + i
    }
}
