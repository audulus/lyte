// args: -c
// expected stdout:
// compilation successful
// assert(true)
// assert(true)
// assert(true)
// assert(true)
// assert(true)

main {
    assert(1 == 1)
    assert(1 != 2)
    assert([1] == [1])
    assert([1] != [2])
    assert([1,2] == [1,2])
}
