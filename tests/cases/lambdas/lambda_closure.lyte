// args: -c
// expected stdout:
// compilation successful
// assert(true)

call(f: i32 → i32) {
    f(0)
}

call_void(f: void → void) {
    f()
}

main {
    var x = 0
    call_void(| | assert(true) )
}