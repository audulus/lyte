// expected stdout:
// ❌ ../tests/cases/lambdas/lambda_escape.lyte:8:18: closure with captured variables cannot be returned (captured addresses would dangle after the frame exits)
//         return (| | x)
//                      ^

get_fn() → void → i32 {
    var x = 0
    return (| | x)
}
