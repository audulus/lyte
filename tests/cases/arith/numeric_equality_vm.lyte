
// args: -r
// expected stdout:
// assert(true)
// assert(true)
// assert(true)
// assert(true)
// assert(true)
// assert(true)
// assert(true)
// assert(true)
// assert(true)
// vm execution successful


main {
    assert(1 == 1)
    assert(2 != 1)
    assert(1 != 2)
    assert(1 < 2)
    assert(2 > 1)
    assert(1 <= 1)
    assert(1 <= 2)
    assert(1 >= 1)
    assert(2 >= 1)
}
