// args: -c
// expected stdout:
// compilation successful
// assert(true)
// assert(true)
// assert(true)
// assert(true)
// assert(true)

main {
    assert(2+2 == 4)
    assert(2*3 == 6)
    assert(10-3 == 7)
    assert(10/2 == 5)
    assert(10%3 == 1)
}
